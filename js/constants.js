// set the scene size
WIDTH = window.innerWidth;
HEIGHT = window.innerHeight;
	
// set some camera attributes
VIEW_ANGLE = 45;
ASPECT = WIDTH / HEIGHT;
NEAR = 0.1;
FAR = 10000;

CLOCK = new THREE.Clock();